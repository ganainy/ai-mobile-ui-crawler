---
phase: 05-test-coverage-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cli/test_crawl_command.py
  - tests/infrastructure/test_mobsf_manager.py
  - tests/integration/test_auth_e2e.py
  - tests/unit/test_traffic_capture_manager.py
  - tests/unit/test_stats_dashboard.py
  - tests/cli/test_delete_command.py
  - tests/cli/test_main.py
  - tests/config/test_config_manager.py
  - tests/core/test_crawl_state_machine.py
  - tests/core/test_pre_crawl_validator.py
  - tests/core/test_runtime_stats_collector.py
  - tests/core/test_stale_run_cleaner.py
  - tests/domain/providers/test_vision_detector.py
  - tests/domain/test_report_generator.py
  - tests/domain/test_traffic_capture_manager.py
  - tests/infrastructure/test_ai_interaction_service.py
  - tests/infrastructure/test_session_folder_manager.py
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "Running pytest on the entire test suite produces zero failures and zero collection errors"
    - "No duplicate test module names exist across the tests/ directory tree"
    - "All 5 previously-failing collection errors are resolved"
  artifacts:
    - path: "tests/cli/test_crawl_command.py"
      provides: "Fixed CLI crawl command tests with correct indentation"
    - path: "tests/infrastructure/test_mobsf_manager.py"
      provides: "Fixed MobSF manager tests with correct imports"
    - path: "tests/unit/"
      provides: "Removed duplicate test modules that cause import conflicts"
  key_links:
    - from: "tests/"
      to: "src/mobile_crawler/"
      via: "All test imports resolve to current source module signatures"
      pattern: "from mobile_crawler\\."
---

<objective>
Fix all 56 failing tests and 5 collection errors to establish a fully green test suite baseline. This is the foundation for all subsequent test coverage work — new tests cannot be trusted if the suite itself is broken.

Purpose: Without a green baseline, CI cannot reliably detect regressions and new test additions have unknown validity. Fixing the existing suite first ensures all future test work stands on solid ground.

Output: Zero test failures, zero collection errors, all duplicate test modules resolved.
</objective>

<execution_context>
@C:/Users/amrmo/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/amrmo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-reliability/05-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
<!-- Current signatures that tests must match -->

From src/mobile_crawler/core/crawl_state_machine.py:
```python
class CrawlState(Enum):
    # Current enum values - tests must use these exact names
```

From src/mobile_crawler/core/pre_crawl_validator.py:
```python
class PreCrawlValidator:
    def __init__(self, config_manager, device_detection, ...): ...
    async def validate(self, ...): ...
```

From src/mobile_crawler/core/stale_run_cleaner.py:
```python
class StaleRunCleaner:
    def __init__(self, ...): ...
```

From src/mobile_crawler/infrastructure/mobsf_manager.py:
```python
# Check current exports - MobSFConfig may have been renamed or removed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 5 collection errors blocking the test suite</name>
  <files>tests/cli/test_crawl_command.py, tests/infrastructure/test_mobsf_manager.py, tests/integration/test_auth_e2e.py, tests/unit/test_traffic_capture_manager.py, tests/unit/test_stats_dashboard.py</files>
  <read_first>
    - tests/cli/test_crawl_command.py (fix IndentationError at line 72 — backslash continuation issue)
    - tests/infrastructure/test_mobsf_manager.py (fix ImportError — check what MobSFConfig was renamed to, or update import)
    - tests/integration/test_auth_e2e.py (fix ModuleNotFoundError — check if device_verifier module exists or remove import)
    - tests/unit/test_traffic_capture_manager.py (duplicate of tests/domain/test_traffic_capture_manager.py — delete this file)
    - tests/unit/test_stats_dashboard.py (duplicate of tests/ui/test_stats_dashboard.py — delete this file)
    - src/mobile_crawler/infrastructure/mobsf_manager.py (check current exports to fix import)
  </read_first>
  <action>
    Fix each collection error:

    1. **tests/cli/test_crawl_command.py**: Fix the IndentationError at line 72. The backslash continuation `\` at the end of a `with patch(...) as` statement is causing unexpected indent. Rewrite the `with` statement to use multiple context managers on separate lines without trailing backslash, or use `contextlib.ExitStack`.

    2. **tests/infrastructure/test_mobsf_manager.py**: The import `from mobile_crawler.infrastructure.mobsf_manager import MobSFConfig` fails. Read `src/mobile_crawler/infrastructure/mobsf_manager.py` to find what is actually exported. If `MobSFConfig` was removed in Phase 2 (Appium removal), either:
       - Update the import to use the correct class name
       - If the class was removed entirely, remove or rewrite the affected test class, keeping tests that exercise remaining functionality

    3. **tests/integration/test_auth_e2e.py**: The import `from tests.integration.device_verifier import ...` fails because the module doesn't exist. Either:
       - Remove this import and the test methods that depend on it
       - Create a minimal `tests/integration/device_verifier.py` stub if tests use it as a helper

    4. **tests/unit/test_traffic_capture_manager.py**: This is a duplicate of `tests/domain/test_traffic_capture_manager.py`. The duplicate causes Python import confusion because both modules have the same `test_traffic_capture_manager` name. DELETE this file entirely — keep the one in `tests/domain/` which is closer to the source module.

    5. **tests/unit/test_stats_dashboard.py**: This is a duplicate of `tests/ui/test_stats_dashboard.py`. Same issue — duplicate module name. DELETE this file entirely — keep the one in `tests/ui/` which matches the source location.

    After fixes, verify collection works:
    ```bash
    cd E:/VS-projects/mobile-crawler/src && python -m pytest --co -q tests/ 2>&1 | tail -5
    ```
    The output should show no ERROR lines and a total test count.
  </action>
  <verify>
    <automated>cd E:/VS-projects/mobile-crawler/src && python -m pytest --co -q tests/ 2>&1 > nul && echo "Collection successful" || echo "Collection failed"</automated>
  </verify>
  <done>
    - tests/cli/test_crawl_command.py collects without error (no IndentationError)
    - tests/infrastructure/test_mobsf_manager.py collects without error (no ImportError)
    - tests/integration/test_auth_e2e.py collects without error (no ModuleNotFoundError)
    - tests/unit/test_traffic_capture_manager.py is deleted
    - tests/unit/test_stats_dashboard.py is deleted
    - `pytest --co -q` shows zero ERROR lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix 56 failing unit and domain tests to match current API signatures</name>
  <files>tests/cli/test_delete_command.py, tests/cli/test_main.py, tests/config/test_config_manager.py, tests/core/test_crawl_state_machine.py, tests/core/test_pre_crawl_validator.py, tests/core/test_runtime_stats_collector.py, tests/core/test_stale_run_cleaner.py, tests/domain/providers/test_vision_detector.py, tests/domain/test_report_generator.py, tests/domain/test_traffic_capture_manager.py, tests/infrastructure/test_ai_interaction_service.py, tests/infrastructure/test_session_folder_manager.py</files>
  <read_first>
    - src/mobile_crawler/core/crawl_state_machine.py (current CrawlState enum values for test_crawl_state_machine fix)
    - src/mobile_crawler/core/pre_crawl_validator.py (current PreCrawlValidator.__init__ signature and validate method)
    - src/mobile_crawler/core/runtime_stats_collector.py (current RuntimeStatsCollector API)
    - src/mobile_crawler/core/stale_run_cleaner.py (current StaleRunCleaner.__init__ signature and methods)
    - src/mobile_crawler/domain/providers/vision_detector.py (current ProviderRegistry and model fetching API)
    - src/mobile_crawler/domain/report_generator.py (current ReportGenerator API)
    - src/mobile_crawler/domain/traffic_capture_manager.py (current TrafficCaptureManager API)
    - src/mobile_crawler/infrastructure/ai_interaction_service.py (current AIInteractionService.parse_ai_response signature)
    - src/mobile_crawler/infrastructure/session_folder_manager.py (current SessionFolderManager API)
    - src/mobile_crawler/infrastructure/device_detection.py (current DeviceDetection API used by pre_crawl_validator)
    - src/mobile_crawler/config/config_manager.py (current ConfigManager.defaults behavior)
    - src/mobile_crawler/cli/commands/delete.py (current delete command signature)
  </read_first>
  <action>
    Fix all 56 failing tests by updating them to match the current source API signatures. The failures are caused by Phases 1-3 refactoring that changed method signatures, constructor parameters, enum values, and return types without updating corresponding tests.

    **Systematic approach:**
    1. Run each failing test file individually to see exact error messages
    2. Read the current source module to find the correct API signature
    3. Update the test to match the current signature
    4. Re-run to verify the specific test passes
    5. Move to the next failing test file

    **Known failure categories (fix each below):**

    **tests/core/test_crawl_state_machine.py (1 failure):**
    - `test_enum_values`: CrawlState enum members have changed. Read `src/mobile_crawler/core/crawl_state_machine.py` and update the expected enum member set to match current values.

    **tests/core/test_pre_crawl_validator.py (15 failures):**
    - All 15 tests likely fail because PreCrawlValidator.__init__ signature changed — it now takes different parameters (e.g., device_detection, model registry). Read the current source, update mock setup and constructor calls in all test methods to match the current signature.

    **tests/core/test_stale_run_cleaner.py (13 failures):**
    - `TestStaleRunCleanerInit`: Constructor signature changed. Update mock setup.
    - `TestIsProcessRunning`: Mock assertions have changed. Update platform-specific process detection mocks.
    - `TestCleanupSingleRun`, `TestMarkRunAsError`, `TestStopOngoingRecordings`, `TestIntegration`: Method signatures or return values changed. Read current source, update mocks.

    **tests/core/test_runtime_stats_collector.py (4 failures):**
    - `test_to_db_dict`, `test_comprehensive_workflow`, `test_record_context_loss`, `test_save_success`: RuntimeStatsCollector API changed. Update field names, method signatures, and expected return values.

    **tests/infrastructure/test_session_folder_manager.py (4 failures):**
    - All 4 tests fail. SessionFolderManager constructor or method signatures changed. Read current source, update fixture setup.

    **tests/domain/test_report_generator.py (4 failures):**
    - ReportGenerator API changed. Update mock setup and expected return types.

    **tests/domain/test_traffic_capture_manager.py (4 failures):**
    - TrafficCaptureManager API changed. Update mock setup and method signatures.

    **tests/domain/providers/test_vision_detector.py (2 failures):**
    - `test_fetch_gemini_models_success`, `test_fetch_gemini_models_failure_fallback`: Provider model fetching API changed. Update mock URLs, response types, and method names.

    **tests/cli/test_delete_command.py (2 failures):**
    - `test_delete_run_not_found`, `test_delete_with_confirmation`: Delete command or RunRepository API changed. Update mock paths and assertions.

    **tests/cli/test_main.py (1 failure):**
    - `test_cli_no_command`: Exit code assertion wrong (expects 0, gets 2). Read the current CLI entry point to determine the correct exit code for no-command invocation.

    **tests/config/test_config_manager.py (1 failure):**
    - `test_precedence_defaults`: Config precedence behavior changed. Update expected values.

    **tests/infrastructure/test_ai_interaction_service.py (1 failure):**
    - `test_parse_ai_response_invalid_action`: AIInteractionService.parse_ai_response signature or behavior changed. Update mock and expected result.

    After fixing all tests, run the full suite to confirm zero failures:
    ```bash
    cd E:/VS-projects/mobile-crawler/src && python -m pytest --ignore=tests/integration --ignore=tests/ui tests/ -v --timeout=60 2>&1 | tail -20
    ```
  </action>
  <verify>
    <automated>cd E:/VS-projects/mobile-crawler/src && python -m pytest --ignore=tests/integration --ignore=tests/ui tests/ -q --tb=no 2>&1 | Select-String "passed|failed|error"</automated>
  </verify>
  <done>
    - All 56 previously-failing tests now pass
    - `pytest --ignore=tests/integration --ignore=tests/ui tests/ -q` shows 0 failed
    - No test collection errors
    - Total pass count increases from 468 to 468+56=524 (approximately)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Source code | Tests import from source modules; signature mismatches cause collection errors |
| Test → SQLite | Some tests use temp databases; must use fresh fixtures per test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Test fixtures modifying shared state | mitigate | Use tmp_path fixtures for filesystem tests; use new DatabaseManager instances per test |
| T-05-02 | Denial of Service | Test infinite loops from mocking errors | accept | Tests have --timeout=60; infinite loops will timeout |
</threat_model>

<verification>
1. `cd E:/VS-projects/mobile-crawler/src && python -m pytest --co -q tests/` — zero ERROR lines
2. `cd E:/VS-projects/mobile-crawler/src && python -m pytest --ignore=tests/integration --ignore=tests/ui tests/ -q --tb=no` — zero failed
3. `ls tests/unit/test_traffic_capture_manager.py` — file not found (deleted)
4. `ls tests/unit/test_stats_dashboard.py` — file not found (deleted)
5. Verify previously-failing tests now pass individually
</verification>

<success_criteria>
- Entire test suite collects without errors
- All tests pass with zero failures (excluding integration tests requiring devices)
- Duplicate test modules are removed
- Green baseline established for subsequent test coverage work
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-reliability/05-01-SUMMARY.md`
</output>