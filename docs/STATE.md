---
author: claude
updated: 2026-09-23
---
# Project State

Entry point for the Obsidian vault (`docs/`). Claude reads this first each session and updates it before finishing substantive work.

## Now
- Issue #24 (crawler never wrote `step_logs`): `CrawlerAgentService` now writes one row per executed tool, numbered like `step_phase_transitions`, with tool name, result summary, input text, success/error, tool duration, and the Executor/FastAgent decision's reasoning and LLM time (LLM time on the batch's first tool only). Screen ids stay null (nothing assigns them). AI-call and step counters stay separate; screenshots now join to steps by timestamp (`screenshots_by_step`), so each decision's screenshot sits on its first tool's line. Merged into main; also fixes #12 (empty per-step data in the Analysis Bundle). Worktree tests need `PYTHONPATH=src` (otherwise they import the main checkout's code). Not tried on a real crawl. See [session](sessions/2026-09-23-write-step-logs.md).
- 2026-09-23: branches for #20, #21, #22 and #23 merged into `main` and pushed with #14–#19; any "not merged to main" in the entries below is out of date. Uncommitted #24 work stays in the `issue-22-phase-timing` worktree. See [session](sessions/2026-09-23-merge-worktrees.md).
- Issue #19: `mobsf-scan RUN_ID` CLI command runs MobSF on a finished run's stored APK (`<session>/apks/<pkg>.apks|.apk`, only present if MobSF already ran for that run); never pulls from the device; clear errors for unknown run / MobSF disabled / no stored APK (checked before Docker auto-start via #15's `ensure_mobsf_running_if_enabled`). `MobSFManager` gained `find_stored_apk`, `perform_complete_scan(apk_path=)`, one shared session-folder resolver; crawl's autostart message helper moved to `cli/docker_autostart_report.py`. Suite green; not tried against a real MobSF container. See [session](sessions/2026-09-23-cli-mobsf-scan.md).
- Issue #18 (committed as 3c439f5): `scenarios` CLI group (`list`, `set`, `add`, `edit`, `remove`, `move`, `generate`) over the same `guided_scenarios::<pkg>` / URL-override settings the GUI uses; `generate` calls `generate_guided_scenarios` like the GUI button, replaces + saves the list and URL override on success, keeps the old list and exits 1 on failure (including exceptions, which the GUI worker also catches); `--provider/--model/--exploration-objective` are single-run overrides via `ConfigManager.override()`. Suite green; `generate` not tried against a real LLM. See [session](sessions/2026-09-23-cli-scenarios.md).
- Issue #17: `crawl --parser-mode/--reasoning-mode/--exploration-objective` added as single-run overrides via new in-memory `ConfigManager.override()` (not persisted). Found that the older `--steps`/`--duration`/`--provider`/`--model`/`--enable-*`/`--no-report` flags DO persist to SQLite despite the issue assuming otherwise; not changed. Suite green, not tried in a real crawl. See [session](sessions/2026-09-23-cli-run-override-flags.md).
- Issue #23 (CLI `crawl --device last` / `--package last`) implemented in worktree branch `issue-23-device-package-last`: the literal `last` resolves to the GUI's persisted `last_device_id` / `last_app_package` (read from `UserConfigStore`); a missing key fails with exit 1 and a stderr message before any run is created; explicit values unchanged. The CLI does not write these keys, so `last` only reflects GUI selections. README example added. Full suite green; code review found no spec gaps (open question: should the CLI also save `last_*` keys?). See [session](sessions/2026-09-23-cli-device-package-last.md).
- Issue #20 (CLI `stats RUN_ID`, `--format table|json`) committed on branch `issue-20-stats` (not merged to main) (worktree `.claude/worktrees/issue-20-stats`): prints the persisted `run_stats` record with the same sections as the GUI "View Stats" dialog; sections and formatting now live in Qt-free `core/run_stats_sections.py`, shared by both; CLI adds a Phases section from `step_phase_transitions`; GUI's always-empty "App Package" row now filled (repository joins `runs`). Live-only items in the issue (screens/min, a11y/OmniParser timings, tool calls/step) are not shown: they aren't persisted. Suite green; not run against the real `crawler.db`. See [session](sessions/2026-09-23-cli-stats.md).
- Issue #21 (CLI `list apps --device ID`) implemented on branch `issue-21-list-apps` (worktree `.claude/worktrees/issue-21-list-apps`): lists the device's third-party packages with app names (via `AppMetadataResolver`), `--format table|json`, `--no-names`, `-n`. Package enumeration moved out of the Qt AppSelector into `infrastructure/installed_apps.py`, which both now use. Suite green; not tried on a device. See [session](sessions/2026-09-23-cli-list-apps.md).
- Issue #22 (phase Timing Breakdown in the Analysis Bundle): `steps.jsonl` lines carry a `timing` object (phase totals, sub-phase metrics like `a11y_ms`/`omniparser_ms`/`manager_llm_ms`, validation retries), `run.json` gets raw `step_phase_transitions`, `analysis.md`/HTML get a "Timing breakdown" section. Pure `build_timing_breakdown` in `domain/step_phase_models.py`; the GUI's copy in `ai_monitor_panel.py` is untouched (out of scope), so the logic is duplicated for now. Review found the crawler never writes `step_logs` (pre-existing: `steps.jsonl` was empty on real runs), so `steps.jsonl` now also has lines for phase-timed steps without a `step_logs` row. Committed on worktree branch `issue-22-phase-timing`, not merged into `main`. Suite green; not checked against a real run's DB. See [session](sessions/2026-09-23-report-phase-timing.md).
- Issue #16 (CLI `crawl --step-by-step`) committed as f91e9b6 (review fixes included: pause state is set before listeners are told, so a buffered Enter can't be dropped; actions recorded only while step-by-step is on). Uncommitted `--parser-mode`/`--reasoning-mode`/`--exploration-objective`, `scenarios` command and MobSF changes in the tree belong to another session, not #16. Details: pauses after each agent step, prints the step's actions (tool, ok/FAIL, duration, summary) to stderr, waits for Enter, then `advance_step`. New `on_step_paused(run_id, step_number, actions)` listener event emitted by the service; stdin reading extracted into a `ConsoleReader` shared with the Human Fallback prompter. Side effect: GUI's previously dead "Step N finished. Paused for review." log line now shows. Suite green; not tried on a device. See [session](sessions/2026-09-23-cli-step-by-step.md).
- Issue #15 (CLI Docker auto-start) implemented, not yet committed: `MobSFDockerService.prepare()` combines ensure_running + wait_for_api_key + save_api_key (GUI's `MobSFStartupWorker` now delegates to it instead of duplicating the sequence); new `infrastructure/docker_autostart.py` (`ensure_mobsf_running_if_enabled` / `ensure_omniparser_running_if_enabled`) is the blocking, Qt-free equivalent of the GUI's enable-check-then-start logic, wired into `crawl.py` before the run starts (failure is a warning, not fatal; containers left running). Written so the still-unbuilt `mobsf-scan RUN_ID` command (separate issue) can reuse the MobSF half. Code review (spec axis) caught a real bug: the success message was printed to stdout, which `JSONEventListener` treats as a newline-delimited-JSON stream for machine consumers — fixed by routing both success and failure to stderr. Full suite green; no typechecker installed in `.venv312` to run; not tried against a real Docker/MobSF/OmniParser container. See [session](sessions/2026-09-22-cli-docker-autostart.md).
- Issue #14 (Human Fallback CLI wiring) implemented: `crawl` now passes a `TerminalHumanPrompter` (blocks on a console prompt for a code or "skip", timeout-driven auto-skip matching the GUI dialog) into `CrawlerLoop`; added `--human-fallback`/`--no-human-fallback` (single-run override, not persisted) threaded through `CrawlerLoop` -> `CrawlerAgentService` -> `HumanFallbackConfig.from_store`. Code review (spec axis) caught a real bug pre-commit: a timed-out prompt left its reader thread blocked on stdin forever, so a second prompt in the same run (e.g. email code falling back to SMS code) would start a second thread and race for the user's answer; fixed by having later calls reuse a still-pending reader instead of starting a new one. Full suite green; not tried on a real device/terminal. Remaining CLI/GUI parity issues #15-#23 still open. See [session](sessions/2026-09-22-cli-gui-parity.md).
- A11y statistics: every capture now reports the Portal fetch time (`a11y_ms`) and whether the a11y tree supplied the elements (`a11y_used`); Stats panel shows "Avg A11y" and "A11y used: X/Y steps". The GUI log viewer never showed the provider's `crawler_agent` INFO lines (trace JSONL had them), so the per-step INFO line is now one `State capture ... | a11y ok|empty|failed (...) Nms (nodes, used) | omniparser skipped|ok|empty|failed Nms` line logged via `mobile_crawler.domain.crawler_agent.tools.ui.provider` (reaches the viewer like the service's "Step N" lines; also DEBUG in the trace); root cause of the drop not found, fix unverified in the GUI. Live only, not persisted (same as OmniParser time). Suite green for `tests/ui` + `tests/domain`; not seen in the real GUI. See [session](sessions/2026-09-20-a11y-stats.md).
- Wireless debugging: user's phone not listed because `adb devices -l` was empty (not paired/connected in adb; PATH also has a second adb from scrcpy after the SDK one); detection code is fine. "No Devices Found" dialog text now includes the wireless pairing steps. Not seen in the GUI. See [session](sessions/2026-09-20-wireless-debugging-hint.md).
- UI element detection review: modes explained, OCR removed (0c84a7e); merge of a11y + OmniParser and a UI-TARS/ShowUI fallback discussed, nothing built, decisions taken (merge only when a11y incomplete, no third parser, no local models); resize not worth it. Built: capture timing breakdown (51eb3c0), unchanged-screenshot parse reuse (c67093d). FOUND: the driver never supplies an a11y tree (`get_ui_tree` returns `[]`), so `boost` == OmniParser and `accessibility` mode is empty; the a11y checks/merge are paused. Decision: revive Portal (upstream renamed to com.mobilerun.portal, AGPL, download pinned APK at setup). Spike done: `state_full` content URI works, ~1.3 s vs ~2.3 s for uiautomator dump. Done: portal.py/portal_client.py updated (2610ff2), driver + provider wired to Portal (effafed), verified on the phone (boost -> source=a11y). Next: the four incomplete-a11y checks, Settings install button, `boost` default, parallel screenshot + a11y fetch. See [session](sessions/2026-09-20-ui-detection-options.md).
- Logging cleanup: crawler-agent log records now reach the GUI with their real level (they were all shown as DEBUG); viewer defaults to INFO; `crawl --log-level`; LLM bodies/token stream and internal chatter moved to DEBUG, per-action and per-step lines to INFO, run banner added. Suite green, not seen in the real GUI. Leftovers listed in [audit](logging-audit.md). See [session](sessions/2026-09-20-logging-audit.md).
- OCR removed (unused): `domain/grounding/`, `easyocr` dependency, Stats "Avg OCR" label, OCR events/args. OmniParser's own internal OCR is untouched. Suite green. See [session](sessions/2026-09-20-remove-ocr.md).
- Log viewer: added text search (substring or regex, shown/total counter) alongside the level filter; the "OmniParser parse call ... took Nms" log is now INFO (was DEBUG). Smoke-tested offscreen, not seen in the real GUI. See [session](sessions/2026-09-20-log-search.md).
- Action Batching (cut tokens/time per crawl step by letting the Executor do several consecutive actions per Manager subgoal): implemented 2026-09-20 (config `max_actions_per_batch`, default 5): Executor batch loop, per-action history, Manager/Executor prompts. Unit-tested, not tried on a device; also fixed boost mode doing a full OmniParser parse after every action (settle wait now hashes screenshots); Settings control still open. See [session](sessions/2026-09-20-action-batching-design.md).
- Live Feed: the Stats panel's Screenshot board is now a live view of the selected device (vendored scrcpy 3.3.4 server over adb, wireless included), with fading element boxes; falls back to the last screenshot. Unit-tested and streamed against a real phone outside a crawl; not yet seen in a full run. See [session](sessions/2026-09-20-live-feed.md), [ADR 0004](adr/0004-drive-vendored-scrcpy-server-directly.md).
- Device selector auto-retries (3x, 1.5 s) at startup before the "No Devices Found" dialog and picks the last-used device when several are connected. Unit-tested, not tried on a device. See [session](sessions/2026-09-20-device-auto-refresh.md).
- Auth scenario text now forbids logging in with the generated sign-up credentials (run 174 tried to log in with them on an email-first web form). Runs 172/173: guard relaunched Headspace during its web login in Brave. Guard now tolerates a browser for up to 40 captures; manager screenshot now comes from the same capture as the parsed elements (was mismatched). Tests pass; not tried on a device. `open_app` app-list lookup (~50s, serial dumpsys) now concurrent + cached. See [session](sessions/2026-09-19-browser-login-guard.md).
- Run 171 died at step 2: executor (Gemini `max_tokens` 512) got `MAX_TOKENS` 3x. Raised LLM profile limits in `crawler_agent_service.py` (manager 8192, executor 4096, app_opener 2048). Verified by the user on a real run. See [session](sessions/2026-09-19-max-tokens-executor.md).
- Notify-hook debug: spurious toasts come from the async global Stop hook firing when the project Stop hook blocks, and an unmatched Notification hook. Fix proposed, not applied. See [session](sessions/2026-09-19-notify-hook-debug.md).
- Test Email field removed; "Test" dropped from the Address and Mobile Number labels (email now from App Account override / Verification Inbox); phone auto-detected from SIM via adb, manual field overrides. Uncommitted; not tried on a real device. See [session](sessions/2026-09-19-remove-test-email-detect-phone.md).
- Old issues #1 and #5 verified fixed and closed; stray `src/=0.4.26` removed. See [session](sessions/2026-09-19-close-old-issues.md).
- Issue #7 (App Accounts) committed as cf3196f: per-app account store, Settings "App Account" group, account injected into the agent goal. See [session](sessions/2026-09-19-app-accounts.md).
- Vault set up 2026-09-19: docs reorganised, shipped feature specs, old plans and fixed-bug handovers deleted (see git history).
- Recent: local OmniParser Docker auto-start at GUI launch; Docker stop dialog / taskbar icon / Stop button polish; App Web Profile -> Guided Scenarios wired end-to-end.

## Next / blockers
- Issue #6 (Run Report + Analysis Bundle): committed as c7f016f (tests not re-run after the last edits, incl. fixes for 7 pre-existing failures); not yet run on a real device or against a live Phoenix/Langfuse server. See [session](sessions/2026-09-19-run-report-analysis-bundle.md).
- Auth / sign-up / OTP support: design agreed 2026-09-19, issues #7-#11 (#11 blocked by #7-#10). #7 App Accounts done ([session](sessions/2026-09-19-app-accounts.md)); #8 Verification Inbox done and pushed (9a000f0, a09e36b; [session](sessions/2026-09-19-verification-inbox.md)); #9 SMS reader committed; #10 Human Fallback pushed (1795e86, [session](sessions/2026-09-19-human-fallback.md)), but its Settings group is still uncommitted in `settings_panel.py` alongside #6-#8 hunks; #11 Authentication scenario implemented ([session](sessions/2026-09-19-authentication-scenario.md)), needs a manual check on a real app with email verification. See [session](sessions/2026-09-19-auth-verification-design.md).
- Test suite sped up to ~3.5 min (2bbbba6); see [session](sessions/2026-09-19-authentication-scenario.md). Keyboard handling: `AndroidDriver.hide_keyboard` (BACK only when `mInputShown=true`) is called by `AndroidStateProvider._dismiss_keyboard` before every screenshot, so the keyboard no longer hides elements or gets parsed by OmniParser; executor prompt updated. Unit-tested, not tried on a device; IME-bounds filter for OmniParser not done. See [session](sessions/2026-09-19-hide-keyboard.md).
- Detection work done 2026-09-20, unit-tested and spot-checked on a phone but not run in a full crawl: Portal-based a11y tree, incomplete-tree checks, parallel capture, unchanged-screen reuse, Settings Portal buttons, `boost` default, phase-duration fix. Open: tune `a11y_checks` from real runs; progress and remaining work are tracked in issue #13 (pushed 2026-09-20, `main` = `22e05f8`). See [session](sessions/2026-09-20-ui-detection-options.md), [ADR 0005](adr/0005-download-pinned-portal-instead-of-vendoring.md).
- Next: run a real crawl with `boost` on the phone and tune `a11y_checks` from the logged "a11y tree incomplete (...)" lines; then verify #6 on a real run, then run the crawler on ~10 health apps and analyse the bundles.

- Pre-commit no longer runs black, ruff or ruff-format (removed 2026-09-20 at the user's request); it still trims whitespace, fixes file endings, checks yaml/large files and regenerates `docs/code`. `[tool.ruff]`/`[tool.black]` in `pyproject.toml` are unchanged.

## Map of the vault
- [Glossary](Glossary.md) - stub pointing at root `CONTEXT.md`
- [code/](code/_index.md) - generated code-structure notes
- [adr/](adr/) - architecture decisions
- [architecture/](architecture/ARCHITECTURE.md) - architecture + README-style guides ([index](architecture/readmes/INDEX.md))
- [sessions/](sessions/) - Claude's dated session logs
- [notes/](notes/) - the user's own notes; Claude reads, never edits

## Code structure
- [code/](code/_index.md) is the generated map of every module: classes, public functions, imports and imported-by links. Regenerated by `scripts/gen_code_notes.py` via the pre-commit hook (`pre-commit install` once per clone). Package summaries are hand-written between the `summary` markers.

## Outside the vault
- Tasks and specs: GitHub issues in `ganainy/ai-mobile-ui-crawler`.
- `specs/013-app-auth-signup`, `specs/015-responsive-ui`: old unfinished specs kept because their status is unclear (auth/signup code appears removed).
- `.planning/`: open text-entry hardening notes and todos (June 2026).
